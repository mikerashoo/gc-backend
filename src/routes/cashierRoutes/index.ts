import {  Router } from "express";       

const cashierRoutes = Router({mergeParams: true});


  
// cashierRoutes.post('/login',  validateData(cashierLoginSchema), cashierLogin); 


export default cashierRoutes;
 