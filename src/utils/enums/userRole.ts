 
  // Users 
  export enum UserRole {
    ADMIN,
    PROVIDER_ADMIN,
    BRANCH_ADMIN,
    CASHIER
  }
  
  // Users 
  export enum ActiveStatus {
    ACTIVE,
    IN_ACTIVE
  }
    
  // Keno Game
  export enum KenoGameStatus {
    NOT_STARTED,
    ON_PLAY,
    DONE
  }
  
  export enum TicketStatus {
    ON_PLAY,
    WIN,
    LOSE
  }
    